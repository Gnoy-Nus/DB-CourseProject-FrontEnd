//home模块的仓库
//state:仓库存数据的地方
const state ={
    count:5,
};
//mutations:修改state的唯一手段
const mutations={};
//actions:处理业务逻辑或异步,但不能直接修改state
const actions={
    
};
//getters:计算
const getters={};


export default {
    state,
    mutations,
    actions,
    getters
};